name: ecommerce_churn_platform
version: "1.0.0"
config-version: 2

profile: ecommerce_churn_platform

model-paths: ["models"]

models:
  ecommerce_churn_platform:
    staging:
      +materialized: view
    marts:
      +materialized: table

